# TaskFlow

TaskFlow 是面向企业后台的分布式任务调度与编排平台。任务按命名空间隔离，
以文件形式持久化；任务依赖组成 DAG，依赖满足后才进入就绪队列；调度器把
就绪任务派发给执行器，执行器持租约运行并通过心跳续约；每轮执行写执行记录，
失败按策略重试并按幂等键去重；大任务分片并行执行，分片结果与游标分别落盘；
并发执行数受配额约束，所有调度事件留审计。

## 构建

```bash
docker build -t taskflow .
```

## 运行

```bash
go run ./cmd/taskflow -addr 127.0.0.1:7791 -data ./data -concurrency 16 -namespaces 4
```

启动后打开 http://127.0.0.1:7791 即可进入控制台，页面包含任务、调度、执行
记录和审计日志四个视图，JSON API 位于 /api 下。

## 数据目录

每个命名空间在数据目录下保存任务写入日志、提交水位、执行记录、租约账本、
重试去重游标、分片完成游标、幂等键目录、配额账本和共享审计日志，全部以
文件形式持久化，数据目录可以直接整体备份。
